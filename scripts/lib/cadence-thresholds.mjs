/**
 * Пороги AI-каденции для italian-estate.com (английский корпус).
 *
 * Движок `scripts/lib/ai-cadence.mjs` и CLI `scripts/cadence-check.mjs` общие с
 * moregroupestate-ru и more-group-website, байт в байт. Разница между сайтами
 * живёт только здесь: язык шаблонов и потолки по коллекциям. Расхождение копий
 * движка ловит `npm run cadence:parity`.
 *
 * КАК МЕНЯТЬ ПОРОГИ. Числа посчитаны командой `node scripts/cadence-check.mjs
 * --calibrate --json` по живому корпусу и выведены одним правилом для всех
 * сайтов, а не на глаз:
 *   maxPer1k        = max(2.5, p90 плотности, округлённый до ближайших 0.5)
 *   maxMetronomePct = p90 метронома, округлённый вверх до кратного 5, в пределах 20..60
 *   minCv           = (p10 CV минус 0.05), округлённый вниз до 0.05, в пределах 0.25..0.35
 * Профиль коллекции заводится только там, где в ней не меньше 8 файлов длиннее
 * её порога слов; считается теми же формулами на данных коллекции. Порог из
 * головы здесь запрещён: гейт по GEO-баллу, выставленный до замера, кончился
 * тем, что машинные тексты набирали 92, а рукописные 78.
 */

export const LANG = 'en';

/** Замер 22.09.2026, 283 файла длиннее 250 слов, ветка cc/cadence-port. */
export const CALIBRATION = {
  measuredAt: '2026-09-22',
  files: 283,
  corpus: {
    per1k: { p50: 1.19, p75: 2.21, p90: 3.28, max: 5.04 },
    metronomePct: { p50: 10, p75: 17, p90: 23, max: 47 },
    cv: { p10: 0.46, p25: 0.51, p50: 0.62 },
  },
  /** Сумма попаданий по категориям на весь корпус. */
  categoryTotals: {
    antithesis: 851,
    colonHook: 31,
    superlativeOpener: 27,
    calque: 6,
    clerical: 3,
    headingHook: 3,
    literary: 1,
    impersonal: 0,
  },
  /**
   * Вывод тот же, что на английском Пхукете: почти всю плотность даёт ОДНА
   * фигура речи, антитеза. Это 851 попадание из 922, то есть 92%, три на файл.
   * Из них 772 в форме «X, not Y» («Underwrite the net, not the headline»).
   * Прежняя проверка (AI_FLUFF_RE в lib/more-content-gate.mjs) ловит только
   * форму «not just ... but», а канцелярит и пафос, которые она сторожит ещё,
   * дают на весь корпус 3 и 1 попадание. То есть сторожили то, чего в корпусе
   * почти нет, и не видели того, что есть.
   *
   * По ритму корпус чище Пхукета: короткой фразой закрыто 10% абзацев против
   * 17% на moregroup.estate, поэтому метроном здесь строже.
   *
   * Проверка на ложные срабатывания: из 10 совпадений антитезы, взятых через
   * равный шаг по всем 851, настоящая антитеза в 9. Одно ложное: уступка
   * «does not itself generate tax but failure to file triggers penalties».
   * Три из девяти несут настоящее различение («IMU on cadastral value, not
   * asking price»), поэтому порог стоит на плотность, а не на присутствие.
   *
   * Языковых разделов, кроме английского, на сайте нет: исключать нечего.
   */
  dominantCategory: 'antithesis',
};

export const CADENCE_THRESHOLDS = {
  // p75 2.21 / p90 3.28 -> 3.5
  maxPer1k: 3.5,
  // метроном p75 17 / p90 23 -> 25
  maxMetronomePct: 25,
  // CV p10 0.46 минус 0.05 = 0.41 -> 0.40, потолок правила 0.35
  minCv: 0.35,
  maxParallel: 3,
  // Повтор n-граммы шумит на шаблонных карточках проектов: только отчёт.
  maxRepeated: null,
  // Ниже 300 слов плотность на 1000 слов перестаёт быть устойчивой: четыре
  // попадания на 264 слова дают 15/1k и выглядят как провал на ровном месте.
  minWords: 300,
};

/**
 * Профили по коллекциям, каждый по своим квантилям. news профиля не получает:
 * в ней 4 файла, меньше восьми, и она меряется базой.
 * Доля помеченных при этих порогах: 54 из 283 (19%), жёстких 0.
 */
export const COLLECTION_PROFILES = {
  // n=121; плотность p75 3.12 / p90 3.76 -> 4.0; метроном p75 19 / p90 23 -> 25;
  // CV p10 0.43 -> 0.35
  guides: { maxPer1k: 4.0, maxMetronomePct: 25, minCv: 0.35 },
  // n=48; плотность p75 2.02 / p90 2.44 -> 2.5 (пол правила); метроном p75 18 / p90 27 -> 30;
  // CV p10 0.50 -> 0.35
  areas: { maxPer1k: 2.5, maxMetronomePct: 30, minCv: 0.35 },
  // n=30; плотность p75 1.43 / p90 2.17 -> 2.5 (пол правила); метроном p75 17 / p90 20.1 -> 25;
  // CV p10 0.50 -> 0.35
  compare: { maxPer1k: 2.5, maxMetronomePct: 25, minCv: 0.35 },
  // карточки, n=65 длиннее 400 слов; плотность p75 1.46 / p90 2.09 -> 2.5 (пол правила);
  // метроном p75 13 / p90 17 -> 20 (пол правила); CV p10 0.56 -> 0.35
  projects: { maxPer1k: 2.5, maxMetronomePct: 20, minCv: 0.35, minWords: 400, maxParallel: 4 },
  // карточки, n=15 длиннее 400 слов; плотность p75 1.68 / p90 1.94 -> 2.5 (пол правила);
  // метроном p75 13 / p90 21 -> 25; CV p10 0.48 -> 0.35
  developers: { maxPer1k: 2.5, maxMetronomePct: 25, minCv: 0.35, minWords: 400, maxParallel: 4 },
};

export function cadenceProfileFor(collection) {
  return { ...CADENCE_THRESHOLDS, ...(COLLECTION_PROFILES[collection] || {}) };
}
