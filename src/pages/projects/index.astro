---
export const prerender = true;
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectCard from '../../components/ProjectCard.astro';
import { formatEur } from '../../lib/cardImage';

const projects = (await getCollection('projects'))
  .filter((p) => !p.data.noindex)
  .sort((a, b) => (a.data.priceFromEUR ?? 0) - (b.data.priceFromEUR ?? 0));

const priced = projects.map((p) => p.data.priceFromEUR).filter((v): v is number => !!v && v > 0);
const minPrice = priced.length ? Math.min(...priced) : 150000;
---
<BaseLayout
  title="Italy Property Projects"
  description="Independent project reviews for foreign buyers: Milan, Puglia, Tuscany, and Lake Como developments with prices and due diligence notes."
>
  <section class="site-container py-14 md:py-20">
    <header class="max-w-2xl mb-12">
      <h1 class="text-3xl">Project reviews</h1>
      <p class="text-lg text-[var(--color-muted)] mt-3">
        <strong>{projects.length} projects</strong> with editorial analysis, from <strong>{formatEur(minPrice) || '€150K'}</strong>, photos, and due diligence notes. Not developer marketing.
      </p>
    </header>
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">
      {projects.map((project) => (
        <ProjectCard
          href={`/projects/${project.id}/`}
          title={project.data.title}
          description={project.data.description}
          image={project.data.heroImage}
          area={project.data.area}
          developer={project.data.developer}
          propertyType={project.data.propertyType}
          status={project.data.status}
          priceFromEur={project.data.priceFromEUR}
        />
      ))}
    </div>
  </section>
</BaseLayout>
